name: deploy-docs
on:
  push:
    branches: [master]
    paths:
      - 'docs/**'
      - 'pair_match/**'
      - 'pyproject.toml'
      - 'uv.lock'
      - '.github/workflows/deploy_docs.yml'
  pull_request:
    branches: [master]
    paths:
      - 'docs/**'
      - 'pair_match/**'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      # Deployment is skipped (the build and doctests still run) until these
      # repository secrets are configured.
      DOCS_HOST: ${{ secrets.DOCS_HOST }}
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --group docs

      - name: Build docs
        run: cd docs && uv run make html SPHINXOPTS="-W --keep-going"

      # Every number the guides quote from the paper is a doctest.
      - name: Run doctests
        run: cd docs && uv run make doctest

      - name: Set up SSH key
        if: github.event_name != 'pull_request' && env.DOCS_HOST != ''
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "${{ secrets.DOCS_DEPLOY_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "${{ secrets.DOCS_HOST }}" >> ~/.ssh/known_hosts

      - name: Deploy docs via rsync
        if: github.event_name != 'pull_request' && env.DOCS_HOST != ''
        run: |
          rsync -az --delete \
            -e "ssh -i ~/.ssh/deploy_key" \
            docs/_build/html/ \
            ${{ secrets.DOCS_USER }}@${{ secrets.DOCS_HOST }}:
